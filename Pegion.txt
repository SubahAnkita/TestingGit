A pegion is a bird.

CLs for a new repository 

git init

git add -A

git commit -m "first commit"

git branch -M main

git remote add origin https://github.com/SubahAnkita/TestingGit.git

git push -u origin main


after some modifications have benn already done to this folder 

CLs for already created repo

git add -A

git commit -m "second commit"

git push -u origin main 
